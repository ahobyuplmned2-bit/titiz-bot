# سياسة التغيير الآمن لبوت Titiz

## القاعدة الأساسية

لا يُعدَّل أي منتج، سعر، صورة، خيار، كلمة مفتاحية، مسار عرض، أو ملف تخزين إلا بعد **طلب صريح ومحدد من صاحب المتجر**. لا تُنفَّذ التعديلات الاستباقية أو التغييرات الجانبية بحجة الإصلاح أو التحسين.

## عند إضافة أو تعديل منتج

يقتصر التعديل على اسم المنتج الذي طلبه صاحب المتجر فقط. لا يجوز تعديل أي سجل آخر في `products.json` أو حذف أي صورة أو تغيير مسار الكاروسيل. قبل الرفع، يجب تمرير اسم المنتج المأذون إلى فحص التغيير، وتشغيل اختبارات الكتالوج والبحث والصور.

## قبل أي رفع إلى GitHub أو Render

يجب تنفيذ الخطوات التالية بالترتيب: حفظ نسخة Git الحالية، تشغيل `change_guard.py` مع أسماء المنتجات المأذون بها، تشغيل اختبارات الكتالوج والبحث والصور، ثم مراجعة قائمة الملفات المتغيرة. إذا ظهر ملف أو منتج غير مأذون، يتوقف الرفع ويُعرض الأمر على صاحب المتجر أولاً.

## أمر فحص التغيير

```bash
python3 change_guard.py --base HEAD --allow-product "اسم المنتج المأذون"
python3 catalog_integrity_guard.py
```

يُستخدم `--allow-product` مرة لكل منتج أذن به صاحب المتجر. إذا لم يكن هناك أمر تعديل منتجات، لا يُمرَّر أي اسم؛ عندها يرفض الفحص أي تغيير في الكتالوج.

كما يفحص `catalog_integrity_guard.py` الحد الأدنى لعدد المنتجات والتشكيلات المحمية وصورها وأسعارها؛ ويمنع الرفع إذا اختفى سجل أو تشكيلة أو صورة محمية.
