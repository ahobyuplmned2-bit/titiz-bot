# سجل مراجعة بوت Titiz

## خط الأساس والنسخة الاحتياطية

- النسخة الاحتياطية قبل المراجعة: `/home/ubuntu/backups/titiz-bot-pre-audit-20260814T222359Z.tar.gz`.
- خط أساس Git قبل الإصلاحات: `7c9169717a7757395dcc05a11498b4cbd07c05bf`.
- تضم النسخة الاحتياطية الشفرة والكتالوج وملفات العملاء وقاعدة SQLite.

## نتائج أولية

- توجد طبقة تفسير دلالي تستدعي نموذجاً متوافقاً مع OpenAI باستخدام `SMART_AI_API_KEY` وتُسجّل حالة النية في `message_events`.
- التوجيه الدلالي يفرّق بين البحث والشراء والسلة والطلبات والدفع والعروض والمندوبة والمحادثة العامة.
- يجب تدقيق سلامة مفاتيح البيئة والردود الاحتياطية، واستدعاء النموذج الفعلي، وتكامل واتساب والأزرار والصور قبل اعتماد المراجعة.

## نتائج تدقيق أولية إضافية

- طبقة واتساب تدعم النصوص والأزرار والقوائم والروابط والصور والكروت الدوارة، وتعيد فشل الإرسال بدل رفع استثناء إلى مسار العميل.
- وجدت قيمة افتراضية حساسة مضمّنة في إعداد رمز وصول واتساب؛ يجب تحويل الإعداد إلى قيمة بيئية إلزامية دون تغيير متغيرات Render.
- يلزم تدقيق التحقق من استقبال webhook، وطول محتوى الرسائل التفاعلية، والمسارات الاحتياطية عند فشل إرسال واتساب أو نموذج الذكاء الاصطناعي.

## نتائج Webhook والبيانات

- يمنع البوت تكرار أحداث واتساب باستخدام سجل دائم ومعرّفات رسائل في الذاكرة، لكنه يعالج حالياً الرسالة الأولى فقط من أي دفعة webhook.
- لا يوجد تحقق توقيع POST لـ webhook عند إعداد سر التطبيق، ويجب إضافة تحقق اختياري لا يوقف الخدمة الحالية قبل ضبط السر في Render.
- قاعدة SQLite تحتوي جداول العملاء والطلبات والمنتجات والسلة والجلسات والرسائل والتذكيرات، مع ترقيات غير هدمية للجداول القديمة.
- يلزم منع الترحيب الافتراضي عند استقبال نوع رسالة غير مدعوم، وتدقيق معالجة الدفعات والردود الصادرة في السجل.

## إصلاحات وفحوص أولية

- تم تحويل مفتاح وصول واتساب المضمن إلى إعداد بيئي إلزامي، مع بقاء القيم المنشورة في Render هي مصدر التشغيل.
- تم توحيد حد الإخراج لطلبات الذكاء الاصطناعي وفق عائلة النموذج، وإضافة JSON Schema صارم للنوايا لمنع النتائج غير المتوقعة.
- تم توسيع النوايا الخدمية للتوصيل والموقع والضمان والكتالوج وخارج اختصاص المتجر، وربطها بمسارات المتجر الصحيحة.
- نجح الفحص الحي للنموذج `gpt-5-mini` عبر الواجهة المضبوطة، وأعاد نية `catalog` بثقة `0.92` لسؤال كتالوج طبيعي.
- نجحت مجموعة الاختبارات الكاملة في نسخة معزولة من المشروع، بما في ذلك الطلبات والسلة والبحث والأخطاء الإملائية والصور والتذكيرات والأزرار والأحداث والأمن والنوايا الخدمية.
- نفذت الحزمة النهائية في نسخة معزولة بعد الإضافات الأخيرة ونجحت جميع الاختبارات وعددها 22 اختباراً، من دون تغيير قاعدة البيانات أو الكتالوج الأصليين.
